/**
 * Global site navbar — used on all pages EXCEPT homepage.
 * Homepage has Hero v3-B's embedded nav. SiteNavbar mounts on /zh/about, /zh/services, etc.
 *
 * Phase 1: Stub returning null. To be implemented in Phase 4.
 */
export function SiteNavbar() {
  return null;
}
